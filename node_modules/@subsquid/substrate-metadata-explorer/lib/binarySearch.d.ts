import { SpecVersionRecord } from "./specVersion";
export interface SearchOptions {
    firstBlock: number;
    lastBlock: number;
    fetch: (heights: number[]) => Promise<SpecVersionRecord[]>;
    progress?: (info: {
        step: number;
        versions: number;
    }) => void;
}
export declare function findSpecVersions(options: SearchOptions): Promise<SpecVersionRecord[]>;
//# sourceMappingURL=binarySearch.d.ts.map