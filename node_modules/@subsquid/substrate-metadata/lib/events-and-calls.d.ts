import { Ti } from "@subsquid/scale-codec";
import { QualifiedName, Type, Variant } from "./types";
export interface Definition extends Variant {
    pallet: string;
}
export declare class Registry {
    private types;
    readonly definitions: Record<QualifiedName, Definition>;
    private hashes;
    constructor(types: Type[], ti: Ti);
    get(name: QualifiedName): Definition;
    getHash(name: QualifiedName): string;
    private computeHash;
}
//# sourceMappingURL=events-and-calls.d.ts.map