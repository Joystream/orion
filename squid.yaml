manifestVersion: subsquid.io/v0.1
name: joystream-orion
version: 1
description: |-
  Joystream Orion v2 backend

build: 

deploy:
  addons:
    postgres: 
  secrets:
    - OPERATOR_SECRET
  processor:
    env:
      ARCHIVE_GATEWAY_HOST: 194.233.167.225
      ARCHIVE_GATEWAY_PORT: 8888
      SUPPORT_NO_CATEGORY_VIDEOS: true
      SUPPORT_NEW_CATEGORIES: true
      KILL_SWITCH_ON: false
      PROCESSOR_PROMETHEUS_PORT: 3000
    cmd: [ "npm", "run", "processor-start" ]
  api:
    env:
      PROCESSOR_HOST: localhost
      PROCESSOR_PROMETHEUS_PORT: 3000
    cmd: [ "npm", "run", "query-node-start" ]